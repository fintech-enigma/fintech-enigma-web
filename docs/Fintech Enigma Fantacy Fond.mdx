# Fintech Enigma Fantasyfond

import { useState, useEffect } from 'react';
import axios from 'axios';
import parse from 'html-react-parser';

import { Line } from 'react-chartjs-2';

export const LineGraf = () => {
    const [ dataset, setDataset ] = useState([]);
    const [ labels, setLabels ] = useState([]);
    useEffect( () => {
        axios.get( ('/dataset/fantacyFond.json') )
        .then(res => {
            setDataset(res.data.map(e => e.price));
            setLabels(res.data.map(e => e.time));
        });
    }, []);
    return (
        <div>
            <Line 
                data={{
                    labels: labels,
                    datasets: [{
                        label: 'Fintech Enigma Fondet',
                        data: dataset,
                        borderWidth: 2,
                        fill: false,
                        lineTension: 0.1,
                        borderColor: 'rgb(75, 192, 192)',
                    }]
                }}
                height={400}
                width={600}
                options={{
                    scaleShowLabels : false,
                    maintainAspectRatio: false,
                    scales: {
                        xAxes: [
                        {
                            ticks: {
                                display: true,
                                autoSkip: true,
                                maxTicksLimit: 10,
                                callback: val => {
                                    const [ day, date, Month ] = val.split(" ");
                                    return `${day.slice(0,3)} ${date} ${Month.slice(0,3)}`;
                                    // return `${date} ${Month.slice(0,3)}`;
                                }
                            }
                        }],
                        y: {
                            beginAtZero: false
                        }
                    },
                    elements: {
                        point:{
                            radius: 0
                        }
                    },
                    hover: {
                        intersect: false,
                        mode: 'index'
                    },
                    plugins: {
                        tooltip: {
                          intersect: false
                        }
                      }
                }}
            />
        </div>
    )
}


<LineGraf></LineGraf>

## 

Gjennom oktober og november konkurrerte vi i DN sitt <a style={{color: "#06ADF4"}} href={'https://investor.dn.no/#!/Fantasyfond/Profil/100593'}>Fantasyfond</a>.

Dette var starten på Fintech sin forvaltningsgruppe, der vi satte sammen vårt første fond mens bedriftskontoen ble opprettet. 

Hver høst arrangerer vi en konkuranse mellom medlemmene i <a style={{color: "#06ADF4"}} href={'https://investor.dn.no/#!/Fantasyfond/Liga/971'}>Fintech Enigma Fantasyliga🤑🤑</a>.

Dette var porteføljen vår:

- NORDIC SEMICONDUCTOR
- CADELER A/S
- KONGSBERG GRUPPEN
- DNB BANK ASA
- GJENSIDIGE FORSIKRING
- EUROPRIS
- AKER BP
- AUSTEVOLL SEAFOOD
- EQUINOR
- SCATEC ASA
- SALMAR