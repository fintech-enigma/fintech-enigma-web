# Analyser
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faDownload } from '@fortawesome/free-solid-svg-icons';
import styles from './css/Rapporter.module.css';

export const DownloadButton = ({ filename, url }) => {
  const handleClick = () => window.open(url, '_blank');
  return (
    <button
      onClick={handleClick}
      className={styles.download}>
      <span>{filename}<FontAwesomeIcon icon={faDownload} style={{ marginRight: '5px' }} /></span>
    </button>
  );
};


<div className={styles.rapporter}>
    <DownloadButton filename={"Stresstest portefølje H1 2025"} url={'/dataset/Stresstest_H1_2025_FE.pdf'} />
    <DownloadButton filename={"Risikoanalyse MOWI 2025"} url={'/dataset/Risikoanalyse av MOWI trend 220925.pdf'} />
</div>